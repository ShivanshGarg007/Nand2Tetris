CHIP Or {
    IN a, b;
    OUT out;

    PARTS:
    Not(in=a, out=nota);                            // not and nand do same function just that not takes 1 input only
    Not(in=b, out=notb);
    Nand(a=nota, b=notb, out=out);
}